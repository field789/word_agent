# Word 文档批注技术难点说明

## 背景

Word Agent 需要对 `.docx` 格式的学生作业添加批注（评论）。`.docx` 本质是一个 ZIP 压缩包，内部包含 `document.xml`（文档内容）、`comments.xml`（批注内容）等 XML 文件。批注需要同时修改三个地方：

1. **`document.xml`** — 在目标文本前后插入 `<w:commentRangeStart>` 和 `<w:commentRangeEnd>` 标记，以及 `<w:commentReference>` 引用
2. **`comments.xml`** — 写入批注的正文内容、作者、日期
3. **`[Content_Types].xml`** 和 **`word/_rels/document.xml.rels`** — 注册 comments.xml 部件

---

## 难点一：python-docx 不原生支持批注

### 问题

`python-docx` 库提供了读取、创建段落和表格等高级 API，但**不支持批注的创建和读取**。它的 API 只封装了 Word 文档的常用功能，批注（comments）属于底层 OpenXML 特性，不在其抽象范围内。

### 影响

不能简单地用 `doc.add_comment(text)` 或 `paragraph.add_comment("批注")` 这样的 API 来添加批注，必须**直接操作底层 XML**。

### 解决方案

采用 `zipfile` 解压 `.docx` → 用 `lxml` 直接修改 XML 文件 → 重新打包的方式。这相当于绕过了 python-docx 的抽象层，直接操作 OpenXML 规范。

---

## 难点二：文件锁定的"幽灵"问题

### 问题

这是本次开发中**最棘手的问题**，表现为：

```
[WinError 5] 拒绝访问。: 'xxx_批改后.docx'
```

当需要批量添加多条批注时，每条批注都要写入同一个 `.docx` 文件。但 `python-docx` 的 `Document()` 构造函数在打开文件时，会获取一个操作系统文件句柄（file handle），并且在 `Document` 对象被垃圾回收前**不会释放**这个句柄。

### 关键发现

即使执行了 `del doc`，Python 的垃圾回收器也不会**立即**释放文件句柄。CPython 的引用计数虽然能立即回收，但底层的 `zipfile` 和 `lxml` 解析器可能持有文件映射，导致操作系统仍然认为文件被占用。

### 错误复现路径

```
第1条批注: 打开文件 → Document() → 读取 → 解压 → 修改 → 重新打包 → 写入文件 → del doc
第2条批注: 打开文件 → Document() → 读取 → ❌ 拒绝访问
                        ↑
                文件仍被前一进程的某些残留句柄锁定
```

### 解决方案（最终版）

**链式临时文件策略**：不反复写入同一个文件，而是每次批注都写入一个**全新的临时文件**，批注链完成后最后一次性复制到目标路径。

```
源文件 → 临时文件1(加批注1) → 临时文件2(加批注2) → ... → 最终输出
```

每个临时文件只被写入一次，不存在"反复打开同一文件"的问题，完美避开文件锁定。

---

## 难点三：段落索引映射

### 问题

学生作业 Word 文档通常包含三部分：

```
【题目】...         ← 段落0-8  不需要批改
【材料】...         ← 段落9-13 不需要批改
【答案】...         ← 段落14+  需要批改和加批注
```

DeepSeek 评判时只看到了【答案】部分，返回的 `paragraph_index` 是**答案部分的相对索引**（0 表示答案第1段），但 Word 文档中实际的段落索引不同（答案第0段对应 Word 第14段）。

### 解决方案

在 `reader.py` 中新增 `extract_answer_part()` 函数，返回 `(answer_text, answer_paragraphs, answer_start_index)`。在 `grading_pipeline.py` 中做索引映射：

```python
rel_idx = comment.get("paragraph_index", 0)  # DeepSeek 返回的（从答案开始）
para_idx = answer_start + rel_idx             # 映射到 Word 文档实际索引
```

---

## 难点四：XML 文本定位与 run 拆分

### 问题

Word 文档中，一个段落的文本可能被拆分成多个 `<w:r>`（run）元素，每个 run 又有自己的 `<w:rPr>`（run 属性，如字体、颜色等）。例如：

```xml
<w:p>
  <w:r><w:rPr><w:b/></w:rPr><w:t>标题</w:t></w:r>
  <w:r><w:t>：7月15日发布会</w:t></w:r>
</w:p>
```

这意味着：

1. 通过文本匹配 run 不可靠——"标题"可能在第一个 run，"发布会"在第二个 run
2. python-docx 内部的 `run._element` 是 lxml 对象，但重新解析后的 XML 树结构不同，无法直接复用

### 解决方案

**放弃文本匹配，改用段落索引 + run 索引定位**。先通过 `python-docx` 的段落列表找到目标段落，然后遍历该段落的 `runs` 找到目标文本所在的 run 索引，最后在重新解析的 XML 树中用同样的索引定位。

---

## 难点五：XML 命名空间处理

### 问题

OpenXML 大量使用 XML 命名空间，例如：

- `w:http://schemas.openxmlformats.org/wordprocessingml/2006/main`
- `r:http://schemas.openxmlformats.org/officeDocument/2006/relationships`

使用 `xml.etree.ElementTree` 时，`addprevious()` 和 `addnext()` 方法不可用，必须使用 `lxml`。而 `lxml` 的命名空间处理方式与标准库不同。

### 解决方案

统一使用 `lxml.etree`，通过 `{{{NS}}}tag` 语法处理命名空间：

```python
NS_W = "http://schemas.openxmlformats.org/wordprocessingml/2006/main"
for r_elem in root.iter(f"{{{NS_W}}}r"):
    ...
```

---

## 难点六：批注 ID 自增与冲突

### 问题

每个批注需要一个唯一的 `w:id`（整数），多个批注的 ID 不能重复。如果只是简单地从 1 开始递增，当文档已有批注时会产生冲突。

### 解决方案

每次添加批注前，扫描 `document.xml` 中的所有 `<w:commentRangeStart>` 和 `comments.xml` 中的所有 `<w:comment>`，提取已存在的 ID，然后取第一个不冲突的值：

```python
existing_ids = set()
for elem in root.iter(f"{{{NS_W}}}commentRangeStart"):
    cid = elem.get(f"{{{NS_W}}}id")
    if cid: existing_ids.add(int(cid))
# 同样扫描 comments.xml
new_id = 1
while new_id in existing_ids: new_id += 1
```

---

## 难点七：文档部件注册

### 问题

如果文档原来没有批注（`comments.xml` 不存在），需要从头创建它，并且还要：

1. 在 `[Content_Types].xml` 中注册 `comments.xml` 的 MIME 类型
2. 在 `word/_rels/document.xml.rels` 中建立批注部件的关系引用

漏掉任何一步，Word 打开文档时都会报错或看不到批注。

### 解决方案

在 `_add_comment_by_indices_inner` 函数中，每次写批注时都检查并补充：

1. 检查 `Content_Types.xml` 是否有 `/word/comments.xml` 的 `Override` 条目
2. 检查 `document.xml.rels` 是否有指向 `comments.xml` 的 `Relationship`
3. 如果不存在则创建，并递增 `rId`

---

## 总结

| 难点 | 严重程度 | 解决方案 |
|------|----------|---------|
| python-docx 不支持批注 | 阻塞 | 直接操作 OpenXML 底层 XML |
| 文件锁定拒绝访问 | 阻塞 | 链式临时文件策略 |
| 段落索引映射 | 中 | extract_answer_part 返回起始索引 |
| XML run 拆分导致定位失败 | 中 | 用 run 索引代替文本匹配 |
| 命名空间处理 | 低 | 统一使用 lxml |
| 批注 ID 冲突 | 低 | 扫描现有 ID 后自增 |
| 文档部件注册 | 中 | 自动补充 Content_Types 和 rels |