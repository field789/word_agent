# Word Agent 目录结构说明

本文档说明 `word_agent/` 下各作业类型目录的用途与使用规则。

## 总览

```
word_agent/
├── 评论/           # 新闻评论作业
├── 消息/           # 消息改写作业
├── 采访/           # 采访提纲作业（20 分制）
├── 策划/           # 策划案作业（40 分制）
└── 源码文件         # *.py / *.md / .gitignore 等
```

每种作业类型目录结构相同，均包含「一稿批改」与「二稿批改」两套流程。

## 一稿批改目录

```
作业类型/
└── 一稿批改/
    ├── Answer/              # 参考答案 PDF（踩点文件）
    ├── homework/            # 学生一稿作业（docx，待批改）
    └── complete_homework/   # 批改输出（按日期归档）
        └── 2026-8-26/       # 当日批改结果
```

| 目录 | 用途 | 使用规则 |
|------|------|----------|
| `Answer/` | 存放参考答案 PDF（如「木铎考研」评论作业01踩点.pdf） | 文件名以序号开头（如 `01`），程序按序号匹配学生作业 |
| `homework/` | 存放学生一稿作业 docx | 把待批改的作业放这里，运行 `python main.py` 自动批改 |
| `complete_homework/` | 批改输出目录 | 程序自动创建，按当日日期归档（如 `2026-8-26/`），**无需手动操作** |

## 二稿批改目录

```
作业类型/
└── 二稿批改/
    ├── 一稿批改/        # 一稿批改后的文件（用户手动放）
    ├── 二稿/            # 学生二稿作业（docx，待批改）
    └── 二稿批改结果/    # 二稿批改输出（按日期归档）
```

| 目录 | 用途 | 使用规则 |
|------|------|----------|
| `一稿批改/` | 存放一稿批改后的 docx | 用户手动放入，程序据此提取一稿评语 |
| `二稿/` | 存放学生二稿作业 docx | 把待批改的二稿放这里，运行 `python main.py` 自动批改 |
| `二稿批改结果/` | 二稿批改输出 | 程序自动创建，按日期归档，**无需手动操作** |

## 使用流程

### 一稿批改
1. 把参考答案 PDF 放入 `作业类型/一稿批改/Answer/`
2. 把学生作业 docx 放入 `作业类型/一稿批改/homework/`
3. 运行 `python main.py`
4. 批改结果输出到 `作业类型/一稿批改/complete_homework/当日日期/`

### 二稿批改
1. 把一稿批改后的 docx 放入 `作业类型/二稿批改/一稿批改/`
2. 把学生二稿 docx 放入 `作业类型/二稿批改/二稿/`
3. 运行 `python main.py`
4. 批改结果输出到 `作业类型/二稿批改/二稿批改结果/当日日期/`

## 重要说明

### 目录中的 `.gitkeep` 文件
- 作业目录中的 `.docx`、`.pdf` 文件**不会提交到 Git**（见 `.gitignore`）
- 每个目录中的 `.gitkeep` 是**占位文件**，用于让 Git 保留空目录结构
- **请勿删除 `.gitkeep`**，否则目录结构会丢失

### 作业文件不入库
- `*.docx`、`*.pdf` 被 `.gitignore` 忽略，**不会上传到远程仓库**
- 这是为了：① 避免仓库过大；② 保护学生作业隐私；③ 参考答案版权
- 换机器时，只需复制作业文件到对应目录即可，无需通过 Git

### 批改人署名
- 所有批改默认署名「xxxx」（`main.py` 中 `author="xxxx"`）

### 作业类型识别
- 程序按**文件夹名**识别作业类型（评论/消息/采访/策划），不是按文件名
- 例如 `采访/一稿批改/homework/` 下的文件即使命名"02 策划..."，也按采访标准批改
